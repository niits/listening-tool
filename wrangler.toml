name = "listening-tool"
compatibility_date = "2026-01-05"

[assets]
directory = "./out"

# Set NEXT_PUBLIC_R2_MODEL_BASE_URL in .env.local (local) or Cloudflare Pages
# dashboard (production) before running `yarn build`.
# Format: https://pub-{hash}.r2.dev/
# Get the URL after: wrangler r2 bucket dev-url enable whisper-models
