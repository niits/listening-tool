Run TypeScript type check and ESLint on the project:

```bash
yarn check
```

If there are errors, fix them before committing. TypeScript errors are in `yarn typecheck`, lint errors in `yarn lint:fix`.
